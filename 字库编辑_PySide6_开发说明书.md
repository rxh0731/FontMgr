# 字库编辑 PySide6 开发说明书

> 文档状态：现行开发规范
>
> 基准日期：2026-08-10
>
> 适用项目：`D:\Code\FontEditor-PySide6`
>
> 旧版参考：`D:\Code\FontEditor`（只读，禁止在迁移过程中修改）

---

## 1. 文档定位

本文档是 FontEditor-PySide6 的现行开发依据，描述当前已确认的业务行为、数据兼容要求、PySide6 技术路线和迁移顺序。

原“经文排版”功能的正式界面名称和默认输出文件基础名统一为“通用经文排版”。模板配置文件固定为 `配置/通用经文排版模板.json`；只有旧的 `配置/排版模板.json` 时必须自动迁移主文件及备份并保留用户模板，新旧文件并存时以新文件为准。模板交换文件的内部类型标识继续兼容旧版本。

资料优先级如下：

1. 本文档中的明确约束；
2. 新项目 `core`、`data`、`services`、`utils` 的实际接口；
3. 旧版 `reference\tk_ui` 的现行业务行为；
4. `reference\旧版开发进度.md` 的完成记录；
5. `reference\字库编辑_V2_Tkinter历史开发说明书.md` 的历史需求。

旧版说明书只用于追溯早期设计，不再作为技术架构、目录结构或完成状态的权威来源。若本文档与实际代码不一致，应先核对旧版稳定程序的真实行为，再更新本文档，禁止凭旧说明书覆盖现有业务逻辑。

---

## 2. 项目目标

在不影响 Tkinter 旧版的前提下，使用 PySide6 重建字库编辑程序，并保持既有字库数据、去杂算法、自动寻优、审核流程和导出结果兼容。

核心目标：

- 解决 Tkinter 画笔、橡皮擦的图像上传与刷新延迟；
- 保留旧版已经稳定的业务规则和数据格式；
- UI 层全面采用 PySide6，不在新主程序中继续使用 Tkinter；
- 业务逻辑与界面解耦，避免再次形成超大窗口类；
- 新旧项目互相独立，可分别开发、运行和回退。

当前新仓库处于“迁移准备基线”：业务层和参考资料已复制，PySide6 主程序尚未建立，不能将已复制的 Tk UI 误认为已完成迁移。

---

## 3. 项目隔离原则

### 3.1 目录边界

- 新版工作目录：`D:\Code\FontEditor-PySide6`
- 旧版工作目录：`D:\Code\FontEditor`
- 新版只允许修改新版目录中的文件。
- 旧版源码、虚拟环境、字库和 Git 仓库均保持原状。
- 如需参考旧版实现，优先读取新版中的 `reference` 副本；确需核对旧项目时只读访问。

### 3.2 Git 规则

- 新版拥有独立 Git 仓库，不关联旧版提交历史。
- 每批功能完成并验证后执行本地提交。
- 未经明确要求不得推送远程仓库。
- 不提交 `.venv`、缓存、日志、构建产物或临时数据。

### 3.3 打包规则

- 开发阶段不自动执行 PyInstaller 打包。
- 只有用户明确要求时才制作或更新便携版。
- Windows 启动时必须在创建 `QApplication` 前设置 FontMgr 专用 AppUserModelID；Qt 运行时图标必须组合打包内彩色方块字 PNG 和多尺寸 ICO。根目录 `FontMgr.spec` 必须同时嵌入 PNG 运行时资源和 ICO 可执行文件资源，确保窗口标题栏、任务栏与 EXE 文件使用同一图标；未经明确要求仍不得执行打包。

---

## 4. 技术架构

### 4.1 技术栈

- Python 3.12
- PySide6 6.10.3
- Pillow
- NumPy
- OpenCV
- scikit-image
- SciPy
- send2trash

依赖以根目录 `requirements.txt` 为准，使用新项目自己的 `.venv`，不得复用或修改旧版虚拟环境。

### 4.2 分层结构

```text
FontEditor-PySide6/
├─ main.py                    # 待建立：PySide6 应用入口
├─ ui/                        # 待建立：PySide6 页面、组件、对话框
├─ core/                      # 图像管线、算法、评分、自动寻优
├─ data/                      # SQLite 数据库、配置和数据访问
├─ services/                  # 导入、审核、字形、设置、清理等业务服务
├─ utils/                     # 图像、路径、日志、字符等通用工具
├─ 配置/                      # 程序数据库及只读导入源
├─ 字库/测试/                 # 独立小型兼容测试数据
├─ prototype/                 # 已验证的 PySide6 画布性能原型
└─ reference/                 # 旧版代码和历史资料，只读参考
```

依赖方向：

```text
ui → services → data/core/utils
```

约束：

- `ui` 不直接拼装数据库记录或旧 JSON，不复制业务校验规则；
- `services` 不依赖 PySide6 控件；
- `core` 中的算法保持可独立测试；
- 后台任务只处理数据，所有控件更新回到 Qt 主线程；
- 新 PySide6 页面不得导入 `reference\tk_ui`。

### 4.3 用户可见内容

界面文案、提示、日志、进度消息、文档和代码注释统一使用简体中文。程序内部类名、函数名和变量名使用英文，并保持既有业务层风格。

所有功能页的返回按钮统一使用“返回首页”，不得使用单独的“返回”。

首页进入新建字库、字库添加、自动优化、手工审核、整体协调、字库导出、通用经文排版和定制经文排版时，页面同步构造及初始数据载入阶段必须由主窗口统一显示系统等待光标。设置光标后先处理一次排除用户输入的 Qt 事件，使 Windows 旋转等待状态立即可见并阻止重复入口操作；页面切换成功、拒绝打开、提前返回或发生异常时都必须恢复此前光标。耗时超过短暂初始化的扫描、解码和生成任务继续使用后台线程与页面进度，不得用等待光标掩盖主线程长时间阻塞。

---

## 5. 字库数据规范

### 5.1 六阶段目录

每个字库目录采用以下阶段：

| 阶段 | 目录 | 用途 |
|---|---|---|
| 1 | `01_源图` | 用户导入文件的原始副本 |
| 2 | `02_灰度底稿` | 灰度、极性和墨色等预处理固化结果 |
| 3 | `03_字形掩码` | 自动优化使用的清洁字形掩码 |
| 4 | `04_自动优化稿` | 模板管线或逐字自动寻优结果 |
| 5 | `05_人工修订稿` | 手工审核与画布编辑结果 |
| 6 | `06_最终成品` | 整体协调后的最终输出结果 |

历史四目录设计已经废弃。新增功能必须识别六阶段目录；如需兼容历史字库，应通过数据访问层集中处理，不能在各页面散布路径判断。

### 5.2 SQLite 数据库

程序级结构化数据统一保存在 `配置/fontmgr.sqlite3`。`application_documents` 以稳定键保存用户设置、算法注册表、通用经文排版模板、定制经文排版模板和字库状态索引；`application_meta` 保存数据库版本。现有配置 JSON 仅在数据库缺少对应记录时导入，来源文件保持不变，后续读写以数据库为准。用户主动执行模板导入导出时仍可使用 JSON 交换文件。

每个字库的唯一运行时数据源为字库目录下的 `font_library.sqlite3`，主要表如下：

| 表 | 用途 |
|---|---|
| `library` | 字库名称、DPI、画布、物理尺寸、元数据、会话、整体协调摘要和修订号 |
| `glyph_variants` | 稳定变体 ID、归属字、序号、状态、原始文件、图像信息和扩展字段 |
| `variant_stage_files` | 六阶段相对文件名与 MD5，不保存图片内容 |
| `optimization_results` | 自动优化方案、得分及结构化结果 |
| `manual_edits` | 人工修订和自由变换参数 |
| `coordination_results` | 单字整体协调与墨色结果 |
| `library_summary` | 首页可复用的字库摘要 |
| `committed_operations` | 可幂等识别的已提交操作 |
| `migration_history` | 初始来源摘要、字形数量和导入时间 |

`GlyphService` 负责数据库访问和内存模型投影。普通保存只提交已变化或已删除的字形，并在同一事务更新字库级信息与修订号；完整替换只用于初始建库和崩溃恢复。图片始终保存在六阶段目录中，不作为 BLOB 写入数据库。

新建字库直接创建 SQLite，不生成同名字库 JSON。若打开仅有旧主 JSON 的字库，系统先在同目录构建临时数据库，完整重建并比较语义数据，再执行 `integrity_check`、`foreign_key_check` 和 WAL 检查点，最后原子安装正式数据库；任一步骤失败都回滚并清理临时库。旧 JSON 保持原样且不再更新，数据库一旦存在便不重复导入。

### 5.3 持久状态、界面阶段与辅助提示

SQLite 数据层使用以下四个持久状态和现有六目录。用户界面不直接显示这组内部状态，而是按当前制作阶段投影：

| 持久状态 | 主要对应目录 | 自动优化页 | 手工审核页 | 整体协调与导出页 |
|---|---|---|---|---|
| `待优化` | `01_原始文件`、`02_灰度底稿`、`03_字形掩码` | `待优化` | 未准入 | 未准入 |
| `待审核` | `04_自动优化稿` | `已优化` | `待审核` | 未准入 |
| `审核通过` | `05_人工修订稿` | `已优化` | `已审核` | `待协调` |
| `成品已生成` | `06_最终成品` | `已优化` | `已审核` | 按成品与墨色契约解析为 `待协调` 或 `已协调` |

建库后的字形必须严格依次经过自动优化、手工审核和整体协调三个制作阶段。每个工作台只显示本阶段已准入的字形和本阶段二态：

- 自动优化：`待优化 / 已优化`，建库字形均由此进入流水线；
- 手工审核：`待审核 / 已审核`，只有已经完成自动优化并具有有效自动稿、人工稿或可证明前序完成的后续文件证据时才准入；
- 整体协调：`待协调 / 已协调`，只有已经完成手工审核并具有有效审核来源或可证明前序完成的有效成品时才准入。

持久状态已经推进和有效阶段文件证据必须同时成立，不能仅凭文件残留或状态文字判定“已优化”“已审核”或“已协调”。一旦记录了更高优先级的人工稿，文件缺失、非法或损坏时不得回退到自动稿掩盖异常。上游重新处理使下游证据失效时，下游准入和完成态必须同步撤销；未知持久状态保守投影为自动优化页的`待优化`并附加文件异常，禁止异常记录从流水线中静默消失。

辅助信息不得替代当前页面的主状态，统一为：

- `未保存修改`：页面内存稿件或协调参数尚未保存；
- `结构需核对`：自动优化结果存在结构保护风险；
- `墨色待确认`：成品墨色尚未满足当前全库契约；
- `人工例外`：用户明确接受该字形的墨色例外；
- `文件异常`：阶段文件、成品路径、持久状态或必要记录异常。

统一解析器必须按当前阶段核对真实源图、自动优化稿、人工审核稿或成品。首页只读扫描兼容尚未由 `GlyphService` 迁移的旧目录名；正式页面加载文件统一使用安全路径解析接口，要求纯文件名、文件真实存在且不是符号链接，非法路径只显示异常而不得读取。

自动优化页的状态筛选固定为“全部（本阶段） / 待优化 / 已优化”；手工审核页固定为“全部（本阶段） / 待审核 / 已审核”，整体协调页和导出页固定为“全部（本阶段） / 待协调 / 已协调”。自动优化、手工审核和整体协调每次打开均默认选择“全部（本阶段）”；各页另保留排序；搜索可作为字形定位和导航保留。自动优化当前筛选没有任何字形时，原图和效果图均显示“暂无图片”；首次显示、尺寸变化及预览模式切换触发的延迟重绘不得把空选择误报为图片读取失败。只有已选中具体字形但对应文件缺失、损坏或无法解码时才显示读取失败。`未保存修改 / 结构需核对 / 墨色待确认 / 人工例外 / 文件异常`只在当前阶段适用的提示列或详情区展示，不提供辅助提示筛选。`墨色达标`和`墨色未启用`只在详情区展示，正常情况下不占列表提示列。四个页面必须复用统一状态投影服务；同字父组只显示本阶段的“已优化 X / Y”“已审核 X / Y”或“已协调 X / Y”及提示数量，不得再生成“部分优化、部分通过、部分协调、部分未保存、部分完成”等派生状态。

四个工作台左侧字形列表的状态过滤与排序控件固定在同一行；搜索框单独占一行，窄窗口下控件允许收缩但不改变选项和排序逻辑。

自动优化、手工审核和整体协调的搜索栏统一在输入框右侧提供“搜索”按钮；用户按回车或点击按钮后执行搜索，输入过程不自动刷新字形列表。搜索范围只包含归属字、字形编号和文件名，不得匹配当前阶段主状态、辅助提示、阶段名称或风险说明。

每次执行非空搜索必须视为一次新的导航：结果列表重建后选中当前排序的第一条匹配字形，并同步刷新中间工作区和整体协调页码，不得继续沿用上一次查询的旧选择。搜索文字被全部删除时立即恢复当前阶段及主状态筛选下的全部字形，同时尽量保留仍然可见的当前字形，无需再次点击搜索。自动优化页面切换搜索结果后，迟到的旧候选任务不得覆盖当前字形；手工审核存在未保存稿时，搜索导致的字形切换继续执行现有保存或放弃确认。

自动优化、手工审核和整体协调的字形树全部列必须使用可交互列宽。三页将当前阶段主状态与辅助提示合并为`状态与提示`列，第一行显示主状态，第二行显示辅助提示，两行分别保留原有颜色；双行文字必须按字体真实基线绘制，父组和子项均须保留完整双行高度，禁止把文字压入半行矩形造成汉字上下笔画裁切。状态筛选只依据第一行主状态。`字形与文件`和`状态与提示`列按表头及真实最大分组数量设置独立保护下限，不得以固定`99/99`预留宽度挤占默认可视区；用户可以加宽但不能缩到主要文字显示不全，得分等其他列允许自由调整。总宽度超过左栏时由字形树自身滚动，不得通过固定列或拉伸列剥夺用户调整能力。

导出不是第四个制作阶段。导出页只纳入已经完成手工审核、进入整体协调阶段的字形，并沿用`待协调 / 已协调`二态；`可导出 / 不可导出`由独立导出审计给出，不替代协调状态，也不参与制作阶段筛选。

首页字库选择表固定显示“字库名称 / DPI / 宽 / 高 / 总字数 / 自动优化 / 手工审核 / 整体协调 / 可导出 / 操作”。DPI、宽和高必须默认完整显示，所有列允许用户调整；首次载入和窗口尺寸变化时，若用户尚未手动调整字库名称列，该列自动填满剩余宽度并使操作列右对齐。自动优化显示 `已优化/总字数`，手工审核显示 `已审核/审核准入数`，整体协调显示 `已协调/协调准入数`；后两阶段未准入的字形不得计入本阶段分母，首页流程卡应另行提示“前序未完成”数量。“可导出”继续是独立快速审计，不能解释为制作阶段完成态。

程序内成功的导入、参数修改、自动优化、手工审核、整体协调和删除操作必须直接更新首页内存摘要，并将摘要及轻量文件系统签名写入程序数据库；正常返回首页只复用内存摘要，禁止启动扫描或显示进度。程序启动时使用字库根目录存在性、字库数据库及 WAL、阶段目录签名验证持久索引；不得将 SQLite 打开和关闭造成的字库根目录时间变化当作业务修改。索引不可信时执行后台深度核对，扫描前后签名不一致时丢弃混合结果并自动补扫一次。新建、追加导入和参数修改只需字库名称时必须使用轻量目录枚举。

状态变更必须通过 `GlyphService` 的阶段方法或对应业务服务完成，以确保图片事务和数据库提交一致。禁止仅修改界面状态或绕过数据访问层直接写库。

自动优化、手工审核、整体协调和字库导出的具体字形项统一仅从字形列表右键菜单提供“修正字形名称”入口，不显示额外按钮。该操作只接受一个有效汉字作为新的归属字，不允许用户直接编辑文件序号、扩展名或阶段目录。系统以稳定的 `变体ID` 保持记录身份，为目标字符分配跨六阶段均未占用的下一个四位序号，并同步更新现有阶段文件、SQLite 变体详情和新旧字形组索引；图片内容、三阶段结果、墨色及得分状态全部保留。文件与数据库更新使用可恢复图片事务，普通失败完整回滚，进程异常退出后按事务阶段恢复。

### 5.4 数据安全

- 程序数据库和字库数据库均启用外键、WAL、`synchronous=FULL`、5 秒忙等待及 `BEGIN IMMEDIATE` 事务；数据库版本不受支持时必须明确报错；
- 旧数据导入必须在字库同目录临时数据库中完成。只有完整语义重建、`integrity_check`、`foreign_key_check` 和检查点全部成功后才可原子安装；失败时不得留下正式数据库或临时 WAL/SHM；
- 新建字库和日常保存不得生成或更新主 JSON、备份 JSON 或 JSONL。旧主 JSON、`.bak` 和旧 JSONL 仅用于首次导入或历史崩溃恢复，来源文件不得改写；
- 自动优化和手工审核按成功字形直接提交 SQLite。用户停止在当前单字事务提交或回滚后生效；数据库提交结果不确定时立即停止整批并保留新图和事务清单，禁止继续处理下一字形；
- 所有会改变阶段图片的交互和批量路径，包括首次生成，都必须使用带新旧 MD5 的 `rollback / rollforward / cleanup` 图片事务。图片事务清单是允许存在的临时 JSON，仅用于跨文件系统与数据库边界的崩溃恢复；恢复完成后清理；
- 同一字库的批处理、交互图片保存和图片事务恢复共用进程内与系统级独占锁。打开字库时先恢复图片事务，再以 SQLite 为运行时数据源；旧 JSONL 只读恢复完成后立即提交数据库，不再创建新日志；
- 整体协调按批次全有或全无提交：准备阶段任一字形失败时不覆盖旧文件；提交阶段使用同目录临时文件、图片事务和单个数据库事务，任一步骤失败时恢复全部旧成品及内存状态；
- 实时图片事务在独占锁内记录旧文件 MD5 和文件标识；文件标识不确定或进程恢复时必须回退完整 MD5，不能以性能优化削弱崩溃恢复校验；
- 删除优先进入回收站，不直接永久删除。测试不得改写真实字库或 `字库\测试` 基准数据，需写入时必须先复制到临时目录。

---

## 6. 已有业务能力

以下能力已存在于复制的业务层或旧版稳定实现中，PySide6 迁移时应复用或等价实现，不应按早期文档重新设计。

### 6.1 字库管理

- 新建字库；
- 删除字库并移入回收站；
- 修改字库规格；
- 列出字库、统计字符数和图片数；
- 自动修复缺失阶段目录；
- 字图追加导入；
- 标准字库导入与导出。

新建字库的成品宽高必须在像素与物理毫米之间双向联动，换算关系固定为`毫米 = 像素 ÷ DPI × 25.4`。用户修改任一像素值或 DPI 时，宽高毫米值立即更新并显示两位小数；用户修改宽或高的毫米值时，只反算对应轴像素并取最近整数。信号绑定必须在页面构造后执行，换算过程使用重入保护，禁止像素与毫米互相递归覆盖。

### 6.2 图片导入

`ImportService` 已支持：

- 递归扫描图片；
- PNG、JPG、JPEG、TIF、TIFF；
- 从文件名提取字符；
- 复制原始图；
- 固化灰度、极性和墨色预处理；
- 模板批量处理；
- 单字逐图自动寻优；
- 目标分数和未达标策略；
- 进度回调和取消；
- 追加导入时避免重复源路径。

导入核对页的大目录载入必须分为两个连续可见阶段：第一阶段在工作线程扫描、校验并生成不超过卡片尺寸的 `QImage` 预览，GUI 线程不得在创建卡片时再次逐张解码源图；扫描达到总数后，进度区继续显示“正在生成校对列表”，用 `QTimer` 按约 12 毫秒时间预算分批、三栏轮转创建卡片并让出事件循环。进度条、取消入口和页面忙碌状态必须保持到最后一批布局完成，期间搜索和导入不可用；取消、返回、重新扫描及迟到工作线程结果必须通过任务代次隔离，不得继续向新页面插入旧卡片。实际写盘导入必须捕获不可变的任务代次、模式、字库名、目标目录和专属取消事件，不得在线程中再次读取页面的同名可变字段；失败清理只允许删除该任务明确创建且位于新版字库根目录直属层级的目录。导入期间请求返回首页时应先发出停止请求，安全保存或回滚当前项目并完成任务回调后再离页，禁止新旧导入任务并发复用同一页面。

两类新建模式：

1. 模板模式：按选定模板批量运行统一管线；
2. 单字寻优模式：每张图片独立选择最优方案，记录得分与方案。

### 6.3 图像管线

`core.pipeline.run_pipeline()` 是当前管线入口。处理顺序为：

1. 预处理；
2. L1 降噪；
3. L2 背景校正；
4. L3 二值化；
5. L4 形态学；
6. L5 连通域过滤；
7. 透明化输出。

预处理支持转灰度、反相和墨色归一。模板和算法参数由 `配置\算法注册表.json` 及现有方案结构驱动。

必须保留的重要行为：

- 灰度母版已固化极性后，后续阶段不得再次修改母版极性；
- 自动寻优方案可以在隔离的算法工作副本中执行墨色归一，但它只服务于分割与评分，不得写回灰度母版；
- 用户可见的整库墨色统一放在整体协调阶段执行，不在逐字自动优化阶段提前固化；
- 拓片类均匀浓墨场景优先使用 Otsu，不能默认使用会削薄粗笔画的 Sauvola；
- L5 删除区必须在灰度图中真正置白，不能只修改随后会丢失的 Alpha；
- 相对面积过滤按文字主体面积推导，并与绝对最小面积共同约束；
- 算法注册表和内置模板是新 UI 的配置来源，不在界面中硬编码两套定义。

### 6.4 自动寻优与评分

`core.optimizer` 提供：

- 噪点分析；
- 缩略图生成；
- 候选方案构建；
- 基础候选评分；
- 梯度派生候选；
- 达标提前停止；
- 最优图、最优方案、得分和达标状态返回。

迁移时保持以下语义：

- 自动优化候选分为“原图直接采用”“仅背景透明”和“寻优优化”三类；
- 候选采用后将“处理类型”写入自动优化方案，供后续审核和结果追溯使用；
- 自动优化先按原图实际内容区分三类来源：正常尺寸图片只有在标准 Alpha 或已解码 Photoshop 图层 Alpha 中存在 `Alpha <= 5` 的强透明背景、且同一个外缘连通域触达画布四边并满足面积或连通率要求时，才属于“已有透明区”；近乎不透明 Alpha、单边透明带、互不连通的角块和纯内部透明块均不得触发直接采用。无有效透明区但近白背景占比、背景亮度和稳定性较高，且中间调、散点和外围大块污染信号较弱时属于“白底已清理”；其余图片属于“未处理截图”。分类结果、置信度和主要判定指标随候选方案记录，分类只决定候选组成和默认选择，不替代结构安全校验；
- 对“已有透明区”的图片，人工工作台第一张提供“原图直接采用”并默认选中，后面仍必须提供至少一张经过正常管线处理且结构安全的“寻优优化”候选；标准 Alpha 和已验证的 Photoshop 图层 Alpha 均保留原始 RGBA 像素，基准前景掩码及极性判断忽略 Alpha 不高于 5 的近透明残留；
- 对“白底已清理”的图片，人工工作台第一张提供“仅背景透明”并默认选中，后面仍必须提供至少一张结构安全的“寻优优化”候选；对“未处理截图”不提供基准候选，只显示寻优候选并默认选中得分最高者；
- Photoshop 图层数据本身不代表已取得透明像素；首版只允许解码 8 位 RGB TIFF 中的单个可见 `normal` 图层、完整 Alpha/R/G/B 通道以及未压缩或 PackBits 数据，且必须严格校验图层边界、通道长度、逐行长度和重新合成的白底图。画布和图层均限制为最多 16 Mi 像素、单边不超过 32768 像素，单通道不超过 24 MiB，图层数据约不超过 96 MiB；多图层、蒙版、ZIP、超限、数据损坏或合成不一致均不得提供“原图直接采用”，应继续按可见合成图的背景稳定性划入“白底已清理”或“未处理截图”；
- 紧裁字形允许以边缘近白像素和高亮背景均值判断稳定白底，不得因文字接触边缘拉低边缘平均灰度而误入完整寻优；检测到高置信外围大块污染时必须否决白底快速路径；
- 自动优化在候选生成前判断文字极性，黑底白字自动校正为白底深字；标准 Alpha 和已解码 Photoshop Alpha 均按 Alpha 大于 5 的有效可见像素判断，校正只作用于工作灰度并写入方案，不修改源文件；
- “仅背景透明”只生成透明背景，不执行去杂或其他寻优；“寻优优化”按正常算法方案处理；
- 基准候选和寻优候选分别计算配额，基准候选不得挤占最低寻优配额；干净或低污染图片的寻优收益不足时只降低排序，不得因此删除最后一张结构安全的寻优候选；缩略图入围项在全尺寸复核失败后应继续补位，直到满足数量或耗尽全部结构安全方案，禁止在只剩基准候选时静默结束；
- 候选说明分别使用绿色、黄色和白色，前两类不进入继续寻优分支；
- 自动方案按原图尺寸和参考笔画宽度写入尺度元数据；缩略图初筛与全尺寸执行按同一方案语义换算核、窗口、半径和面积参数；
- 只有明确低对比图片才增加受限 CLAHE、背景校正、Triangle 和 Phansalkar 路线，正常对比图片不得无条件进入增强分支；
- 评分同时记录笔画、深墨核心、背景清洁、拓扑稳定和前景占比；深墨核心、主体覆盖、骨架和笔画宽度执行硬保护，端点和孔洞只在非重污染且参考结构可信时作为硬门槛，其他情况保留为评分软约束；之后按 Pareto 层级、综合分和候选差异稳定排序；
- 结构保护用于候选安全分级，不得把有效处理结果误报成算法无结果：只要存在原尺寸结构安全的寻优候选，就只返回安全候选；如果全部寻优候选都未通过结构保护，则从非空真实处理结果中按“笔画覆盖完整优先、风险等级优先、同级得分优先”选择风险候选，并写入 `结构复核={状态, 阶段, 原因, 风险等级}`。风险候选仍属于 `寻优优化`，允许查看、继续探索和保存，但人工采用前必须明确确认；综合得分只用于排序，不代表结构安全；
- 重污染路线包含“双阈值种子重建”，以深墨核心为种子、宽松阈值区域为支持恢复相连浅笔画；束搜索中间层使用无骨架快速评分，决赛层和最终候选必须执行完整结构评分；
- 密集细噪路线必须从多阈值稳定性估算主体笔画尺度和噪声尺度，不得使用字符名、固定方向或单字模板；合法小点和长细笔需同时满足墨色、形态、尺度和邻近证据。证据不足的歧义部件只进入保形档，均衡与强清理档使用高置信主体；所有档位只能删除基准前景，低置信时逐像素回退，并保护窄桥、孔洞和稳定深墨核心；
- L5“主体外污染过滤”不得依赖字符名、模板或固定上下左右位置；必须按笔画宽度和主体尺度分析连通域簇、主体间隔、外围位置、薄长单块或不规则碎片带。只有多项证据达到高置信门槛时才删除，低置信结果保持输入不变，并保护规则点画、长底笔和面积接近的合法分离部件；
- 评分参考掩码在主体筛选前复用同一空间污染分析，但只接受置信度不低于 0.92 的结果；深墨核心 94%、主体覆盖 82% 等既有硬门槛不得为适配污染清理而放宽；
- 缩略图只用于候选初筛，最终候选必须回到原图尺寸重新执行、重新评分和重新排序；
- 目标跑分是质量线，不等同于绝对准入线；
- 未达标策略支持“入库并标记”和“跳过不入库”；
- 未达标但入库的图片状态为 `待复核`；
- 审核页支持低分优先；
- 每字得分和方案必须持久化。

评分逻辑的重要约束：

- 参考文字主体使用主要连通域，避免把散点当作应保留笔画；
- 参考掩码连通域需满足相对面积下限；
- 非文字连通域按尺寸分级惩罚；
- 明显浅于主体文字的贴字噪点仍需惩罚；
- 诊断归档图片时不能再次墨色归一，否则会污染评分结论。

学习模型与传统寻优的协作规则：

- 处理引擎默认是 `none / 无学习模型`，此时直接进入传统图像管线；
- 后续学习模型通过适配器注册，不把模型推理代码写死在页面或寻优器中；
- 模型可输出 `probability_mask`、`binary_mask` 或 `clean_gray`，服务层统一校验尺寸、类型和取值范围，再转换为寻优工作灰度；
- 模型只负责提供更可靠的前景依据，传统管线继续负责候选派生、清理、结构保护、评分、排序和全尺寸复核；
- 评分参考仍使用反相校正后的原始灰度，避免模型输出同时充当答案和评分依据；
- 一条探索分支固定模型标识、版本、输出类型、模型指纹、推理参数和配置指纹；其中任一项变化时必须从原图重新生成基础候选；
- 切换处理引擎会结束当前分支并从原图开始，模型失败时可切回“无学习模型”，不会删除有效字形记录；
- 有效标准 Alpha 或经严格解码及白底合成校验的 Photoshop 图层 Alpha，其“原图直接采用”路线独立于学习模型；不能把模型掩码或尚未解码为真实 Alpha 的 Photoshop 图层元数据伪装成原图透明结果；
- 推理缓存必须按源图、极性规则、模型版本与指纹、配置和输出类型隔离，并设置容量上限。
- 自动优化页提供“批量自动优化”整库入口：只处理当前字库中状态为 `待优化` 的字形，并固定使用启动时选中的处理引擎上下文。“已有透明区”使用批量快速路径直接采用原图，“白底已清理”只执行背景透明化，两类基准候选无效时才回退完整寻优；“未处理截图”执行完整寻优。完整寻优必须按“安全候选优先、低风险优先、同风险等级高分优先”选择图片、灰度母版、掩码、尺寸和前景均有效的候选；零安全候选时采用风险最低的真实处理结果，结构复核元数据随方案保存并进入待手工审核，批量汇总单列“结构需人工核对”数量而不计失败。批量快速路径不得为最终不会采用的候选执行学习模型、完整寻优或结构/骨架评分，而以原图分类置信度作为可追溯的轻量评分；人工工作台中的基准候选仍执行完整结构评分。完整寻优回退只请求最终 1 个寻优结果，不得按人工工作台的 8 个候选配额执行。单结果复核不得先评分最终不会返回的原图基准；全尺寸管线产生完全相同的掩码时必须按完整内容键复用评分，但仍逐方案执行结构保护判定。原始文件名为空等损坏的待优化记录不得从批次中静默遗漏；单字生成或保存失败时继续后续字形并在结束时汇总，已经进入后续阶段的结果不得覆盖；当前探索分支有未保存选择时必须先保存或取消。
- 交互候选生成、探索或保存只有在源文件缺失或损坏、所有算法都无法形成有效文字前景、候选分层数据无效或算法异常时才算真正失败；结构保护未通过但前景有效的结果必须作为需人工核对的风险候选返回。真正失败时不得自动删除字形记录或原图，也不得推进处理状态；页面必须明确提示记录和原图均已保留，可稍后重试或人工核对。删除字形记录只能由明确的用户删除操作触发。批量流程将单项失败纳入汇总并继续后续字形。
- 整库自动优化在后台顺序执行，耗时算法开始前即报告当前字形，保存完成后再推进已完成数；页面使用独立任务进度显示百分比、当前数/总数和当前字形，并提供“停止批量优化”。停止采用协作式取消，不强制终止工作线程：算法应在分类、模型、候选生成和全尺寸复核的阶段边界检查取消请求；尚未提交的当前字形不保存，已经成功保存的结果继续保留，保存事务已经开始时必须完整提交或回滚，剩余字形保持 `待优化`。运行期间禁止重复启动、切换处理引擎、切换字形、返回首页或关闭主窗口；正在停止时显示明确状态。正常完成、用户停止、部分失败、工作线程异常、任务启动失败或结果刷新失败均必须恢复返回首页、完成按钮、处理引擎、搜索和列表导航。
- 批量自动优化不得逐字复制整库状态或重写完整快照；每个成功字形直接增量提交 SQLite，并按 5.4 使用图片事务和独占锁。完整寻优的批量专用包装只保留最终会采用的一个寻优候选，不能裁剪影响安全分级、风险等级或得分排序的核心预筛和原尺寸结构复核；人工工作台仍生成完整候选集合。PNG 编码必须同步取得落盘字节 MD5，避免为摘要重新读取三层临时文件。界面进度通知限制为约 10 次/秒但必须保留首项、末项、停止和失败状态；批处理线程日志按约 64 KiB 分块刷新，任务结束和异常退出上下文时强制刷新，批次结束日志汇总候选生成、结果保存、状态提交与总耗时。
- 整库自动优化启动前必须说明自动判断和自动选择可能造成笔画缺失、污点残留或边缘变化，并明确原图不修改；确认按钮固定为“确定 / 取消”，默认按钮及 `Esc` 均为取消。没有待优化字形时直接提示阶段完成，不显示风险确认。

### 6.5 审核与协调

已有业务服务包括：

- `ReviewService`：审核统计、分页查询、状态筛选和审核结果提交；
- `GlyphService`：读取变体、保存编辑稿、完成审核、复制最终结果；
- `AdjustmentService`：整体协调预览、共享几何渲染、批量成品生成、墨色统一和事务回滚；
- `CleanupService`：中间文件清理；
- `SettingsService`：设置读写。

PySide6 页面应调用这些服务，不应直接复刻文件操作。

墨色归一的最终职责位于整体协调：

- 审核稿先转换为单色字库的临时视觉墨量表达：`coverage = Alpha × (1 - RGB 亮度 / 255)`，工作副本使用黑色 RGB 与 coverage Alpha；该转换不修改原图、灰度母版、优化稿或审核稿；
- 几何变换与全部重采样完成后再执行目标墨色校正。启用正式批量协调时，可按每个字形的最终几何结果重新统计全库基准，避免缩放、旋转、斜切和扭曲改变边缘后沿用过期基准；
- 单字墨色取 coverage 不低于 `16` 的有效前景第 70 百分位，全库基准取全部有效单字值的中位数；阈值以下的插值尾像素不参与笔画墨色统计，但仍保留在成品中；
- 目标校正使用支持域内的比例增益，并以量化后实测值二分复核最接近目标的结果，不再受旧版 Gamma `0.75~1.35` 范围限制；纯二值、RGB、Alpha 以及 RGB+Alpha 混合来源使用同一视觉口径；
- 默认容差为 `3`。预览、单字精调和保存调用同一最终画布处理路径，并逐字记录方法与版本、像素类型、前景阈值、调整方式、增益、调整前后墨色、目标偏差、容差、是否达标、状态和人工例外；
- 单字可选择“跟随全库”“保留本字”或“人工例外”。“保留本字”未达标时仍是待确认；只有用户明确选择“人工例外”后，该字才可作为已接受例外计入完成；
- 用户可以关闭“统一墨色”；关闭时必须从审核来源重新生成，不能复用上一次已归一的成品；
- 墨色开关提供全库一致的处理目标，但分页或单字保存只应用到按钮明确对应的字形；范围外成品、状态和几何草稿均保持不变，直到所在页另行保存或用户执行“完成整体协调”；
- 只有全部审核字形都有真实成品、记录的方法版本和当前基准一致，且每字均已达标或被人工接受为例外时，才标记整库墨色统一完成；任一待确认项都会阻止完整导出；
- 导出只读取并审计整体协调成品，不得再次执行墨色归一。旧字库缺少新版方法、版本或统计时，原有“墨色统一完成”标记自动失效，必须重新协调。

---

## 7. PySide6 界面范围

### 7.1 页面清单

按旧版现有能力迁移：

1. 首页与字库列表；
2. 新建字库；
3. 字图追加导入；
4. 字库规格修改；
5. 模板生成与算法配置；
6. 字库审核；
7. 手工审核；
8. 整体协调；
9. 最终确认与导出；
10. 设置；
11. 字库说明；
12. 模型与算法说明。

界面视觉可按 PySide6 重新设计，但业务语义、状态推进、文件结果和用户确认步骤必须与稳定版兼容。

### 7.2 主窗口建议

- 使用单一 `QApplication`；
- 主窗口建议使用 `QMainWindow`；
- 页面切换建议使用 `QStackedWidget` 或路由控制器；
- 全局主题集中管理，禁止每个页面重复硬编码完整样式表；
- 长任务使用 `QThreadPool/QRunnable` 或工作对象配合 `QThread`；
- 工作线程通过信号上报进度、结果和错误；
- 所有对话框、提示和进度文案使用中文。

#### 7.2.1 统一字形列表

自动优化、手工审核、整体协调和字库导出的左侧字形列表采用统一父子树结构，但状态名称必须服从当前页面语义：

- 自动优化使用“字形与文件 / 状态与提示 / 得分”，手工审核和整体协调使用“字形与文件 / 状态与提示”；`状态与提示`第一行分别显示当前阶段的优化、审核或协调主状态，第二行显示辅助提示，两行分别保留原有颜色，筛选只依据第一行。导出仍在协调状态与提示之外另设独立“导出”审计列；其他两页不增加无业务含义的得分列；
- 自动优化阶段状态为“已优化”的字形默认只读显示原图与现有优化稿，右侧“当前方案”标题下必须提供醒目的“重新优化此字形”入口。用户确认后只从原图生成候选，不得立即改写任何阶段文件；采用并保存新候选时复用完整图片事务覆盖优化稿，并将状态退回待审核。若该字形已有人工审核稿或成品，确认信息必须明确说明保存新候选会撤销这些后续结果，用户需要重新完成手工审核和整体协调；
- 父节点显示“字（N个字形）”，默认展开、加粗且只负责归类，鼠标或键盘导航不得令父节点取代当前字形；单字形字符也必须保留父子结构；
- 子节点统一显示 `38×38` 白底真实阶段图片、“字形N · 完整文件名”和页面专属状态，紧凑行高基准约为 46～52 像素，长文件名只按“字形与文件”列的实际文字区域增加换行高度，其他列宽度不得令子项异常膨胀；
- 自动优化以建库字形为入口；手工审核列表只纳入已完成自动优化的字形；整体协调和导出列表只纳入已完成手工审核的字形。未完成前序阶段的字形不得以额外状态混入后续页面；
- 自动优化优先显示自动优化稿，否则显示经过极性校正的原图；手工审核优先人工稿，否则自动优化稿；整体协调依次使用当前协调预览、成品、人工稿和自动优化稿；导出优先最终成品，待协调时显示当前可用的审核来源，但协调完成状态仍只由持久状态、最终成品和墨色契约共同决定；
- 每页状态筛选只包含“全部（本阶段）”与本阶段待/已二态，并保留排序；搜索可保留为导航。辅助提示只展示，不提供提示筛选；
- 搜索或状态筛选命中子项时保留对应父组，显示数量、进度、上一字形和下一字形均按实际子项计算；
- 大列表不得在 GUI 线程同步解码整库图片，应只加载可见区和受控预取范围；工作线程只生成 `QImage`，主线程生成 `QPixmap/QIcon`，缓存必须有界，并以规范路径、修改时间、文件大小和页面代次防止旧图错配；
- 自动优化交互候选缓存按 Pillow 解码像素与 NumPy 底层存储的实际占用估算，同一底层数据只计一次；默认最多保留 16 组且总计不超过 192 MiB，按 LRU 淘汰。单组候选超过预算时仍允许显示和保存，但不得进入缓存；
- 自动优化在“待优化”筛选下保存时按保存前捕获的后继字形 ID 前进，不得因刷新和再次移动而跳过字形。

### 7.3 整体协调工作台

整体协调采用左侧字形列表、中间双模式工作区、右侧参数与固定保存区的三栏布局。

- 左侧只纳入已经完成手工审核的字形，表头使用“协调状态”，本页只显示`待协调 / 已协调`；状态筛选为“全部（本阶段） / 待协调 / 已协调”，默认“全部（本阶段）”，辅助提示只展示不筛选；
- 整库比较按中栏可视区响应式显示：`1600×900` 为 `5×3`，`1100×720` 为 `4×2`；统一正方形田字格与图像坐标，末页保留固定空槽，不得出现横向滚动；
- 整库比较中仅选中字形显示与单字精调相同的虚线四边形、八个方形手柄和旋转手柄；框内移动、四角等比缩放、四边单轴拉压、旋转、`Shift` 约束、`Alt` 中心缩放、`Ctrl + 四角` 透视扭曲和 `Ctrl + 四边` 斜切必须直接复用 `ReviewCanvas` 的变换会话。旋转或扭曲后不得退化为 Alpha 轴对齐包围盒；连续拖动只生成一条撤销记录。拖动期间只用当前缓存位图和四边形做即时投影，不运行 Pillow/OpenCV 高质量协调预览，松手后只重算一次；
- 鼠标位于当前选中字形卡片时，滚轮按每格约 `5%` 调整等比缩放，范围仍为 `5%–500%`；连续滚轮事件合并为一条撤销记录，停滚后只生成一次高质量预览。切换字形、分页、保存、完成整体协调或离开页面前必须先结束尚未提交的拖动或滚轮会话；控制框、右侧参数、单字精调画布和保存服务共享同一份变换数据；
- 双击字形或按 Enter 进入单字精调，复用 `ReviewCanvas` 的移动、等比缩放、横纵拉压、旋转、四角扭曲、斜切、撤销重做、适合窗口、`1:1`、原稿查看、田字格、白底/透明底和空格抓手，不包含画笔、橡皮或取墨；
- 参照字下拉列表固定按归属字拼音和文件名自然顺序排列，不受左侧字形列表及比较墙当前排序影响；参照字可固定并以半透明层叠加，参照叠加与原稿查看均属视图状态，不写入调整，不产生脏状态或撤销记录；
- 审核或成品字形达到后台分析阈值时，页面不得在 GUI 线程同步扫描全库图片；全库尺寸与墨色基准必须在工作线程逐字统计，载入进度持续显示当前数、总数、字形和百分比，完成前禁用编辑和提交但允许返回首页。审核来源的有效性验证和 RGBA 读取必须合并为单次解码；后台失败时使用已有基准并在进度区明确显示失败原因；
- 比较模式保存语义为“保存本页”，精调模式为“保存本字”和“保存并下一字”；完成整体协调及离开页面时检查全部未保存字形，确认按钮固定为“保存修改”“放弃修改”“取消”；
- “保存本页”只能原子保存点击时当前页的字形，“保存本字”和“保存并下一字”只能保存当前字形；当前墨色设置与几何调整均只应用到本次按钮对应范围，范围外字形不得生成或覆盖成品、不得推进状态，已有几何草稿继续保留；全库处理仅由“批量整体协调”执行；
- 已保存的非零变换重新载入后必须作为干净基线，画布撤销栈和重做栈均为空；预览与最终保存必须复用同一几何渲染规则，避免视觉中心或边界发生像素偏移；
- 同批次的成品图片和 SQLite 状态必须事务提交，预处理或提交阶段任一失败时恢复全部旧成品和内存状态；主窗口关闭事件与返回首页使用同一未保存确认规则。
- “批量整体协调”启动前必须以中文显示本批总字形数并确认；任务在后台依次报告准备、渲染和提交阶段，使用独立于左栏长期完成度的进度条显示百分比、当前数/总数和当前字形。任务运行期间禁用编辑、保存、返回和快捷键；任一记录缺少变体 ID 或原始文件名，或者审核稿没有透明度加权后的有效非白前景时必须整批失败，且失败时不得把后台状态应用到页面。批次成功、业务失败、工作线程异常或批次后页面刷新异常均必须先恢复返回首页、批量按钮、编辑区和快捷键。
- “批量整体协调”的启动确认必须说明默认参数、未逐字比较、墨色统一、页面草稿和更新已有成品等风险，按钮固定为“确定 / 取消”，默认按钮及 `Esc` 均为取消。任务区必须提供“停止整体协调”；停止请求和最终提交使用线程安全门控，提交前受理停止时删除临时文件且本批不提交，已有成品、索引和页面草稿保持不变；进入提交后按钮显示“正在提交…”，迟到的停止请求不受理，事务继续完整提交或回滚。

---

### 7.4 定制经文排版工作台

首页“通用经文排版”之后提供独立的“定制经文排版”入口。左侧正文、系统或外部字图来源、缺字核对和中间高清预览复用通用排版的安全读取、后台进度、鼠标缩放及平移链路；右侧使用“第 1 版 / 第 2 版 / … / ＋ / 输出参数”单层页签，避免重复导航层级。

- 排版固定为竖排、从右向左；正文每个非空行对应一列，连续一个或多个空行分隔版面；
- 每块版面独立设置 DPI、单元格高宽、基准行距、基准列字数、固定列距、大框和画布四边距；
- 基准列总高度为 `基准列字数 × 单元格高 + (基准列字数 - 1) × 基准行距`；实际占用格数达到基准列字数的 90% 时，先在保持单元格高不变的情况下重新均分行距，计算结果小于零时将行距固定为零并等比缩小该列单元格高度；
- 实际占用格数严格低于基准列字数的 90% 时，不再把短列拉满基准高度，而是按正文从右向左的顺序继承前一列最终采用的单元格高度和行距；连续短列沿用同一节奏，第一列没有可继承对象时使用版面默认单元格高度和基准行距。固定列距不随列字数变化；
- 每版最后一个正文行对应视觉最左侧的末列。末列实际占用格数少于基准列字数时，无论是否达到 90%，均继承右侧前一列最终采用的单元格高度和行距；等于基准列字数时使用标准参数，大于基准列字数时继续按基准总高度压缩行距，必要时缩小单元格高度。单列版面没有前列可继承时使用版面默认参数；
- 定制排版贴入字图时按物理毫米尺寸换算到当前画布 DPI，不比较源图 DPI 与输出 DPI。系统字库以建库元数据中的成品毫米宽高及标准画布像素比例为准，对称扩展成品沿用同一毫米/像素比例；外部字图从文件分辨率元数据推导物理尺寸，缺少有效物理信息时保留原像素尺寸作为兼容回退。高清预览、资源预估和 PSD/PSB 字形图层必须共用这一尺寸结果；该规则不改变通用经文排版的“按源图尺寸 / 相对单元格”选择；
- 正文版数与参数版数必须实时显示。正文多于参数时，多出的正文显示“无参数 · 忽略”且不生成文件；参数多于正文时，多余参数保留但不生成空白文件；数量不一致只提示，不阻止其余匹配版面生成；
- 新增版面参数时，用户可选择程序默认值或复制任意已有版面参数；至少保留一块参数；
- 新增版面页签始终插入“＋”之前，使全局输出参数保持最后一页；预览翻页与版面页签双向同步，删除当前版的入口位于版面参数页内；
- 右侧版面参数按基础尺寸、框线与边距、其他规则、逐列等高结果分区显示；基础尺寸采用双列对齐，四向边距采用矩阵对齐，右栏默认宽度为 `640px` 且仍允许用户拖动分隔线调整；
- 模板保存全部版面参数、标点和尺寸标注规则，使用独立文件 `配置/定制经文排版模板.json`；内置默认模板硬编码托底，配置缺失时自动恢复；
- 预览和正式输出复用通用排版的字图索引、逐字透明裁边、PSD/PSB 自动判断、压缩选择、同名冲突处理、逐版子进程、原子替换、进度及立即停止机制；预览缩放下限取 `10%` 与适合窗口真实比例中的较小值，已有位图分辨率足够或缩放已到边界时不得重复启动高清任务；共享服务通过逐单元格几何支持动态列高，不改变通用排版的普通规则网格。

## 8. 手工审核画布规范

手工审核画布是第一优先级，也是采用 PySide6 的主要性能原因。

### 8.1 已验证结论

独立原型 `prototype\PySide6画布性能测试.py` 已在真实机器上验证流畅。测试证明持久 `QImage`、`QPainter` 和脏矩形刷新可以绕开旧版 Tkinter 的 `PhotoImage` 创建及上传瓶颈。

### 8.2 必须采用的渲染架构

- 高清数据层使用持久 `QImage`；
- 画笔和橡皮擦直接修改工作图像的局部区域；
- 绘制使用 `QPainter`；
- 仅调用 `update(dirty_rect)` 请求脏矩形重绘；
- `paintEvent()` 只绘制需要显示的内容，不在每帧执行完整 Pillow 转换；
- 屏幕缩放结果允许缓存；
- 一笔结束时只创建撤销快照或命令，不触发无必要的整图重建；
- 光标、控制柄和辅助线作为独立覆盖层绘制。

### 8.3 禁止回退的做法

- 每个鼠标移动事件创建整张 `QImage` 或 `QPixmap`；
- 每帧执行 Pillow → QImage → QPixmap 的整图转换；
- 每帧对整张高清图做 LANCZOS 缩放；
- 松笔时无条件完整重渲染；
- 在主线程执行批量导入或长时间算法任务；
- 为了“迁移方便”在 Qt 事件循环内嵌入 Tkinter 画布。

### 8.4 功能兼容要求

手工审核最终需覆盖：

- 字形与变体选择；
- 三栏核对和分页；
- 移动、缩放、旋转和扭曲；
- 画笔、橡皮擦和笔刷大小；
- 田字格、边界框、控制柄和实时预览；
- 撤销、重做和重置；
- 自动对齐与缩放；
- 保存编辑稿及状态推进；
- 本阶段状态筛选、搜索导航和低分优先等排序；辅助提示只展示；
- 中文性能日志。

当前 PySide6 手工审核工作台已经完成以下核心能力：

- 三栏布局固定为左侧字形缩略图与进度、中间大画布、右侧工具参数与审核操作；
- 左侧只纳入已经完成自动优化的字形，使用“审核状态”列；状态筛选固定为“全部（本阶段） / 待审核 / 已审核”，默认“全部（本阶段）”，搜索用于导航并保留排序。审核完成后本页显示“已审核”，内部仍保存为兼容状态`审核通过`；结构风险和文件异常等辅助提示只展示，不提供提示筛选；
- 中间工具栏在 `1100×720` 最小窗口仍完整可辨认，画布支持白底、透明棋盘格、田字格、适应窗口和 1:1；田字格使用浅红色，外框为实线，内部中心线和对角线为虚线；
- 画布不显示外围 130% 红框，但保留与田字格同中心的 130% 不可见越界编辑范围；范围内可补画并透明扩图，范围外禁止落笔；
- 按住空格时，鼠标或绘图板笔尖临时切换为抓手平移画布，松开空格恢复当前工具；临时平移只改变视图，不修改稿件、未保存状态或撤销栈；
- 自由变换覆盖移动、四角等比缩放、四边单轴拉伸或压缩、旋转、`Shift` 等比约束、`Alt` 中心缩放、`Ctrl + 四角` 透视扭曲和 `Ctrl + 四边` 斜切；旋转后沿字形局部轴操作，对角、对边或中心锚点固定；
- 变换渲染顺序固定为缩放与拉伸、OpenCV 透视、旋转扩边，非法、自交、退化或过大的四边形必须拒绝；变换或笔画越界时对称扩展透明画布而不裁切；
- 变换控件只提交当前字段，范围与画布契约一致；滑块拖动实时更新读数并在释放时一次提交，避免覆盖其他高级参数或产生大量撤销记录；
- 首次载入状态为 `待审核` 的自动优化稿时，按有效文字包围盒计算 `max(文字宽 / 字库画布宽, 文字高 / 字库画布高)`；严格小于 `60%` 或严格大于 `120%` 时等比缩放到 `95%`，`60%` 与 `120%` 边界及区间内保持原尺寸。自动归一只烘焙首次自动稿，保持宽高比和原视觉中心，不受手工变换 `5%–500%` 范围限制；已有人工审核稿不得重复归一，按住查看原稿仍显示归一前图像，交互审核与批量审核必须复用同一处理函数；
- 画笔、橡皮和主墨色取样使用持久 `QImage`，拖动过程按脏矩形刷新；
- 画笔和橡皮使用 Qt 原生绘图板事件，默认以压力线性控制笔触粗细，最轻笔触比例可调且默认 20%；无压感能力时退回固定笔宽，翻转笔尾可临时擦除且不改变当前工具；
- 已处理的绘图板事件按设备与对应合成鼠标序列去重，真实鼠标、触摸屏及绘图笔在自由变换工具中的鼠标兼容输入保持可用；丢失抬笔、窗口失焦或活动笔画期间撤销时统一提交输入边界后清理状态；
- 像素编辑与自由变换共享撤销、重做和恢复保存基线，切换到像素工具或保存时只烘焙一次变换；
- 保存始终生成手工审核稿，采用同目录临时文件、旧稿与旧成品备份及失败回滚，SQLite、图片和内存状态保持一致；全透明或纯白空白稿不得保存或审核通过，回滚恢复失败时不得删除唯一备份，必须保留并向用户报告路径；
- 切换字形、切换字库和返回首页均保护未保存修改，用户取消切库时主窗口不得误切页面。
- 手工审核页提供“批量手工审核”整库入口：从全部真实变体状态中取得 `待审核` 项，不得因列表无法显示缺失来源或变体脱离字形组索引而漏过；优先使用已有人工修订稿，否则按当前审核画布的变换与透明画布规则从自动优化稿生成审核稿，再推进为 `审核通过`。缺失、损坏、全透明或纯白来源保持原状态并进入失败汇总，单字采用独立事务且失败后继续其他字形。
- 批量审核开始前必须保护当前未保存画布，只允许保存后继续或取消；存在待审核字形时还必须说明自动通过不能替代逐字判断，以及可能遗漏字形缺损、误删笔画、残留污点、大小失衡或视觉中心偏移，风险确认按钮固定为“确定 / 取消”，默认按钮及 `Esc` 均为取消；没有待审核项时直接提示完成。任务在后台顺序执行，进度区显示当前字形、当前数/总数和百分比，并提供“停止批量审核”；已经完成的单字保留，当前单字事务必须完整提交或回滚，未处理字形继续保持 `待审核`。运行期间禁止保存、审核、快捷键、切换字形、返回首页或关闭主窗口。正常完成、用户停止、部分失败、工作线程异常、任务启动失败或结果刷新失败必须经过统一收尾，恢复返回首页、完成按钮、编辑区和快捷键。
- 批量审核不得逐字深复制或保存整库快照，必须使用单字局部快照和 5.4 规定的图片事务、SQLite 增量提交与独占锁。同尺寸无变换稿直接复用，首次尺寸归一必须复用同一 RGBA 数据和文字包围盒；已有有效人工稿不得重新编码或覆盖，只校验内容、更新实际 MD5 并推进状态。进度限频但必须保留最终完成或停止位置，日志分块缓冲并在正常或异常收尾时强制刷新，批次日志汇总图像与审核保存、状态提交和总耗时。

OpenCV 透视变换行为应继续保留。旧版曾使用 Pillow 透视变换并出现奇异分母和 NaN，大角度扭曲不得退回该实现。

---

## 9. 迁移阶段与验收

### 阶段 1：应用骨架

内容：

- 建立 `main.py`、`QApplication` 和主窗口；
- 建立主题、页面路由、异常处理和日志入口；
- 接通测试字库读取；
- 建立后台任务统一封装。

验收：程序可独立启动和退出，首页能读取 `字库\测试`，不导入 Tkinter，不访问旧版目录。

### 阶段 2：手工审核画布

内容：

- 从已验证原型提炼可复用画布组件；
- 实现载图、显示、画笔、橡皮擦和脏矩形刷新；
- 迁移变换、撤销、保存和审核状态；
- 接入 `GlyphService` 与 `ReviewService`。

验收：真实大图连续绘制保持流畅，保存结果和旧版兼容，原图不被覆盖，性能日志无秒级整图转换。

### 阶段 3：审核工作流

内容：

- 迁移字库审核、三栏核对、筛选、分页和低分优先；
- 连接手工审核页面；
- 完成自动优化“待优化 / 已优化”、手工审核“待审核 / 已审核”、整体协调“待协调 / 已协调”的三阶段流水线闭环，并将结构、墨色、未保存和文件问题作为只展示、不筛选的辅助提示。

验收：可在复制的临时测试字库完成一轮审核，阶段图片和 SQLite 状态同步一致。

### 阶段 4：字库管理与导入

内容：

- 首页、新建字库、追加导入和规格修改；
- 模板选择、单字寻优、进度、取消和错误报告；
- 保持六阶段目录，字库结构化数据写入 SQLite。

验收：模板模式与单字模式均可端到端创建字库；中途取消不产生损坏数据。

### 阶段 5：模板与算法配置

内容：

- 按注册表生成预处理和五层算法界面；
- 参数编辑、模板保存、说明和预览；
- 复用现有管线与优化器。

验收：同一输入与同一方案在新旧版产生等价结果；不重复执行已固化预处理。

### 阶段 6：协调、确认与导出

内容：

- 整体协调双模式工作台与分页、逐字保存；
- 最终确认；
- 标准字库导入导出；
- 中间文件清理。

验收：状态从审核通过推进到成品已生成，最终文件完整且数据库无悬空路径。

当前字库导出工作台已经完成：

- 页面采用左侧分组字形列表、中间全库成品总览、右侧导出设置的三栏结构，入口为主窗口栈内正式页面；导出不构成第四个制作阶段，左栏只纳入已经完成手工审核、进入整体协调阶段的字形，已有成品显示最终真实图片，待协调项显示当前有效审核来源，父组不可成为当前字形；
- 中间总览默认每行 8 个字形，用户可调整列数；项目按视口宽度均分且仅纵向滚动，不因滚动条出现而改变列数；
- 全库条目由虚拟 `QListView` 模型承载，只解码可见区域及前后预取范围；后台只产生 `QImage`，最多使用 2 个独立解码线程，非方形图片按原始比例居中显示；
- 缩略图缓存按 `QImage` 实际字节数计费并采用 LRU 淘汰，预算根据系统总物理内存和当前可用内存动态计算，低内存时缩小预算并降为单线程；
- 左栏另使用 2 个后台线程、可见区加 8 项预取和 256 项图标 LRU；缓存签名包含规范路径、修改时间与文件大小，同路径图片更新或损坏文件修复后必须重新解码；
- 导出统一使用 PNG，支持按照字库 DPI 与画布尺寸保存、精确裁掉全透明边缘保存，以及自定义 DPI、宽、高后按完整图片或实际文字区域等比缩放居中；
- 左侧和中间的单字状态沿用整体协调的“待协调 / 已协调”，状态筛选固定为“全部（本阶段） / 待协调 / 已协调”，搜索用于导航并保留排序；辅助提示只展示，不提供提示筛选。顶部“可导出 / 不可导出”是独立审计结论，不替代协调状态；绿色圆点配合“全库可导出”文字表示全部字形均已协调且通过完整导出审计，红色圆点配合“全库尚不可导出”文字表示仍有未协调或异常字形，颜色不是唯一信息来源；
- 页面打开后在后台逐项解码成品并核对已有 MD5，核对期间禁止启动导出，离页可取消核对；红色状态下仅在用户中文确认后导出深度核对通过的成品，没有有效成品时禁止导出；
- 导出只从 `06_最终成品` 读取，拒绝越界成品路径、异常大源图、字库内部输出目录和不可预测的同名覆盖；部分导出使用核对通过的变体白名单，批次先写入临时目录，转换、取消或提交失败时不留下部分结果并显示精简失败详情。

### 阶段 7：回归与交付

内容：

- 功能回归、异常路径、边界图和性能验证；
- 更新用户说明；
- 用户明确要求后再制作便携版。

验收：测试字库完整跑通；旧版项目 Git 状态和文件哈希无变化；新版无 Tkinter 运行依赖。

---

## 10. 测试要求

### 10.1 基础测试

- 业务层模块导入和语法检查；
- 空图、全白、全黑、极小图和超大图；
- PNG、JPEG、TIF、TIFF；
- 黑底白字和白底黑字；
- 中文路径、长路径和重复文件名；
- 取消、异常、磁盘写入失败、数据库损坏和非法导入数据；
- 多变体字符和缺失阶段目录。

### 10.2 算法回归

- Otsu、百分位、Sauvola、黑帽和连通域过滤；
- 雪花椒盐噪点；
- 浅灰贴字噪点；
- 粗笔画保留；
- 墨色归一与逐图极性判断；
- 自动候选、派生候选、目标分数和未达标策略；
- 模型输出协议、版本与配置隔离、模型失败回退和探索分支固定；
- 自适应尺度、低对比门控、拓扑硬保护、Pareto 排序和全尺寸复评分；
- 密集细噪的三档单调重建、低置信逐像素回退、浅墨纹理点画、斜向短笔、连接末笔、无固定点画数量上限、锚点歧义、窄桥/孔洞保护和真实污染样本清理；
- 整体协调响应式布局、固定槽位、视图只读层、保存基线、中文离开与窗口关闭确认、白底视觉墨量基准、纯二值与混合像素校正、保存后复测、人工例外、分页保存范围隔离、共享几何像素一致性、批量事务回滚和完成状态；
- 同一固定样本的新旧结果对比。

### 10.3 画布性能

日志至少记录：

- 图片载入；
- 首帧显示；
- 每笔总耗时；
- 局部像素处理；
- 脏矩形刷新；
- 松笔提交；
- 保存和状态更新。

重点观察平均值、中位数、P95、最大值及超过 16.67 ms 的帧数。不得只凭主观流畅度删除性能日志；稳定后可降低日志级别。

### 10.4 保存性能与一致性

- 手工审核的普通保存和审核通过必须在后台线程执行；保存期间禁用会改变当前稿件或页面状态的操作，成功后只合并当前字形的状态并局部刷新列表，不得重新扫描或重建全库字形列表。退出未保存确认中的“保存修改”仍须同步完成，保证对话框返回时数据已经持久化。
- 首页摘要按字形缓存三个阶段的计数贡献。程序内单字状态变化只重算该字形并增量更新字库摘要；只有摘要契约、墨色基准或外部数据发生变化时才允许全量失效。三个阶段状态必须在一次内部状态解析后完成投影，禁止对同一字形重复解析工作流状态。
- 整体协调保存使用包含源图内容、画布、DPI、几何变换、墨色契约和渲染版本的生成签名；签名和文件均有效时直接复用已有成品。需要重绘时最多使用两个工作线程并行生成和验证临时图片，全部成功后再进入单一事务；协调摘要只深度核对本次变化的字形，事务清单只写入必要的最终前滚状态。任何快速路径不得削弱原子替换、备份恢复、取消清理和崩溃恢复能力。

---

## 11. 明确废弃或不采用的设计

- Tkinter 作为新版主界面技术；
- Tk `Canvas` + `PhotoImage` 的像素编辑刷新链路；
- 四阶段字库目录；
- 旧版平铺模板格式作为新模板编辑模型；
- UI 内复制管线、评分或状态推进逻辑；
- 每笔完整高清重渲染；
- 使用固定延时猜测布局完成；
- Pillow 透视变换用于当前扭曲功能；
- 自动修改旧版代码或旧版业务字库；
- 未经要求自动打包或推送。

---

## 12. 新会话开始步骤

新会话进入 `D:\Code\FontEditor-PySide6` 后依次执行：

1. 阅读 `AGENTS.md`；
2. 阅读本文档；
3. 阅读 `README.md`；
4. 查看 Git 状态；
5. 只在需要时查阅 `reference`；
6. 阅读 README 中的“当前工作区交接”，先核对未提交差异，再从现有迁移进度继续，不修改旧版目录。

建议的新会话指令：

> 请继续开发 `D:\Code\FontEditor-PySide6`。先阅读 `AGENTS.md`、`字库编辑_PySide6_开发说明书.md` 和 `README.md`，核对当前 Git 状态与未提交差异，保留测试字库现有变化，并从导入核对、自动优化工作台和新增回归测试的当前进度继续。`D:\Code\FontEditor` 只读且必须保持不变。

### 12.1 2026-08-14 至 2026-08-15 工作区交接

- 当前自动优化阶段检查点命名为“自动优化0812”；该检查点在 81 项回归通过时建立，后续研究从本节记录的算法与性能状态继续。
- 当前 `main` 分支存在未提交的导入页、自动优化页、优化服务、主题、开发文档和测试字库修改；新会话不得擅自还原。
- 首页第一行快捷入口从左到右固定为“通用经文排版 / 文字统计 / 模板工坊 / 使用说明 / 设置”，不显示“新建字库”；制作流程固定为 `0 新建字库`、`1 字库添加`、`2 自动优化`、`3 手工审核`、`4 整体协调`和导出。第 `0` 步置于标题为“创建新的字库项目”的独立分组框，后五个所选字库功能连同字库标题置于“制作流程”分组框；两框必须等高，六张入口卡必须等高、等宽并使用相同标题字号和按钮尺寸，沿底边对齐。字库选择表固定为十列，以 `已完成数/本阶段分母` 合并显示自动优化、手工审核和整体协调进度；DPI、宽、高默认完整，所有列可调，字库名称列在用户未调整时自动填充剩余宽度。“可导出”是独立快速审计，要求成品记录为纯文件名且文件实际存在，但不得在首页执行图像解码或 MD5 深度核对。首页摘要由程序内业务操作实时维护并持久化，正常返回首页绝不扫描；只有启动索引失效或用户通过“重新核对”明确确认时才深度核对。没有字库时仅第 `0` 步可用，其余入口禁用并显示“等待新建字库”；选中字库后六步保持单行可用，`1100×720` 下页面不产生水平滚动，字库表格在空间不足时允许自身水平滚动。
- 通用经文排版使用三栏工作台。左栏“包含标点符号”关闭后，标点从分版、预览和输出中统一排除，检查结果同时统计正文总字数、去重字数、标点数及与所选字库比较后的缺字数；本系统字库按字形组、变体序号及真实可读成品文件读取，不因墨色统一未完成而误报整库缺字，外部目录按文件名首字归类并兼容 PNG/TIF/TIFF/JPG/JPEG/BMP/WEBP/PSD，同字多文件均作为变体。右栏上方为“排版参数”（含“版面 / 文字与段落”页签），下方为同级“输出参数”区块；来源、文字缩放和段落方式分别使用互斥单选，未选方式及相关参数禁用。大框与画布四边距都以最外侧单元格为基准，特殊行距和特殊列距独立配置；源图尺寸模式保留全局缩放和可关闭的自动放大缩小，相对单元格模式使用目标填充率。输出目录默认当前系统桌面，文件名默认“通用经文排版”；单版不加尾号，多版自动追加从 `-01` 开始的逐版编号。用户输入的基础名称必须完整保留，即使以数字结尾也不得把末尾数字当作版号替换；例如多版时 `11` 固定生成 `11-01.psd`、`11-02.psd`。尺寸标注图层列出全部版面、缩放及段落参数。输出参数允许启用或关闭 PSD 压缩，所选方式必须同时作用于合成预览和全部像素图层。正式输出在建立独立图层的同时维护整版合成图，保存前直接写入 PSD 兼容预览并跳过 `psd-tools` 的通用多图层合成；图层层级为背景、框线、文字、尺寸标注，内嵌预览与图层强制合成最多允许逐通道 1 级取整差异。生成日志逐版记录画布创建、框线、字形图层、尺寸标注、兼容预览编码、PSD 写盘、文件替换、内存回收与总耗时，并记录整批阶段耗时汇总。单版生成在独立子进程中完成，停止时立即终止当前进程并清理临时 PSD；已完成版面和已有目标文件不受影响。中栏预览使用持久 `QImage` 和脏区 `QPainter`，连续缩放期间即时改变画布尺寸，停稳后仅在现有位图分辨率不足时在工作线程按视窗、缩放及屏幕像素比重绘当前页；单页高清位图限制约 1200 万像素，不缓存多页或创建缩放后的整页副本。缩放上限为 `800%`，下限取 `10%` 与适合窗口真实比例中的较小值；超大版面必须能通过滚轮缩回完整页面，到达边界或有效渲染尺寸不变时不得启动重复任务。末版压缩后只剩单列或单行时，预览画布必须允许缩到实际比例，并在任意控件比例内保持文档宽高比居中绘制；多余区域使用深色留白，禁止铺满控件造成横向或纵向拉伸。
- 通用经文排版方向采用两个正交参数：排版方向为“竖排 / 横排”，行进方向为“从左到右 / 从右到左”，默认及旧模板迁移值固定为“竖排、从右到左”。竖排按列内从上到下填充，横排按行内填充；行进方向分别控制列顺序或行内字序。横排的段落强制换轨、首尾经题换轨和末版裁空分别解释为换行、经题另起一行和删除空行。字图缩放与田字格居中必须继续使用完整源画布；确定左上角后，以缩放图的 Alpha 非零包围盒裁掉外围全透明像素，并把包围盒偏移累加到 PSD 图层坐标，保证合成位置逐像素不变。预览可继续直接合成完整 RGBA，正式 PSD 不得为每个文字保存无用透明边缘。
- 通用经文排版模板采用顶部居中的下拉选择。下拉选择后立即应用并刷新右侧参数；用户修改任一参数后，下拉框立即显示“自定义模板”，并启用“模板保存”。保存窗口支持“保存为新模板”或“替换现有模板”，替换列表只包含用户模板；内置“默认模板”不可覆盖、改名或删除，顶部也不提供删除入口。模板内容固定为版面、文字缩放、段落、标点、末版压缩和尺寸标注规则；正文、字图来源、本系统字库或外部路径、输出目录、文件名和 PSD 压缩不属于版面模板。默认模板参数严格硬编码在程序中，配置文件缺失、损坏或默认记录被外部篡改时使用硬编码参数托底，并在需要时重建有效版本 2 配置；旧版版本 1 模板必须直接兼容，后续用户写入时原子迁移为带稳定 ID、说明、创建时间和修改时间的版本 2 结构。
- 导入页三栏校对支持正常项编辑、简繁歧义选择、人工红框输入、重复图片对照和异常提示；大目录扫描会在后台同步准备缩略图，扫描结束后以第二阶段进度分批生成三栏卡片，全部布局完成前保持页面可响应且不开放导入，取消或重新扫描不会混入旧代次结果。
- 自动优化候选分类及透明图像语义已接入服务和页面，候选处理类型在保存时写入自动优化方案。
- 已有有效透明区使用交互与批量共享的增强 Alpha 轻度去杂：按主体连通域累计面积估算笔画宽度，可靠主体和邻近抗锯齿边缘必须完整保留；重复出现、面积和跨度均远小于点画尺度且累计质量有限的高 Alpha 微小域可以作为散点场清理，不能再因单个高 Alpha 像素保护整个杂点。单个或少量可能属于点画的高 Alpha 小域继续保守保留；其他待删域仍综合面积、长宽跨度、核心距离、Alpha 分位值、单域质量和总移除质量执行结构保护。轻度清理后必须再次统计高 Alpha 微小连通域，安全且清理充分时批量快速采用；仍有成片残留、候选数据无效或删除存在结构歧义时关闭快速候选并转入完整寻优，不得把“删除安全”误当成“清理充分”。单字候选同时显示增强轻度去杂和完整寻优结果。判定是通用规则，不得按具体字符或文件名设置特例。对“小品-庆兰”1841张含透明层源图的只读分流为原图快速采用181张、增强轻度去杂1385张、完整寻优275张，快速路径覆盖约85%。
- Photoshop TIFF 已接入保守的单图层 Alpha 解码；正常尺寸图片要求同一个 `Alpha <= 5` 外缘强透明连通域触达画布四边，外缘背景占画布至少 12% 时允许“口、国、因”等大内腔字形不受内部透明字腔的连通率拖累，近乎不透明 Alpha、单边透明带、断开角块、小边缘瑕疵和纯内部透明块均不构成有效背景。最终对 1607 个待优化文件全量只读核查无读取错误，分类为已有有效透明区 1069 个、白底已清理 149 个、未处理截图 389 个，批量快速路径覆盖 1218 个（75.79%）；1006 个 Photoshop Alpha 成功解码，其中 1005 个通过有效性判定、1 个安全降级。剩余完整寻优样本最大仅 295×300，全部待优化源图最大为 2806120 像素，低于 16 Mi 像素解码上限。30 张有效 Alpha TIFF 的快速基准构造由 5.313 秒降为 0.659 秒，约快 8.1 倍；`體-0001.tif` 由改造前 164～182 秒的完整寻优降为 0.400～0.474 秒直接采用。不符合支持边界的文件继续安全回退。
- 自动优化已接入反相原图自动校正，校正信息会显示并持久化到候选方案。
- 自动优化页按字符分组显示真实缩略图、完整文件名、得分和状态，父组不可成为当前字形；大列表只在后台分批加载可见缩略图并使用 512 项文件签名 LRU，“待优化”筛选下保存后按保存前捕获的后继 ID 前进，不跳过字形。左栏默认宽度收窄，得分按整数显示且列宽只需容纳“100分”，文件名支持换行完整显示；整库进度和上一/下一字形导航固定在左栏底部；中栏取消重复的当前字形与原始文件信息，原图和选中效果预览顶边与左右栏对齐且高度提高约 20%，预览工具位于两张预览图之间；候选提示固定在候选框底部，候选图按当前窗口宽度动态确定每行槽位且最多 4 列，候选不足时沿用完整槽位并保留后续空位，候选框、左栏和右栏底边对齐；处理引擎选择、状态和模型设置统一位于右栏，当前方案的候选名称、综合得分、处理引擎、基础路线和算法组合合并到同一个滚动文本框，方案探索、处理记录和保存操作保持固定。
- “待优化”筛选下保存最后一个待优化字形后，列表为空时必须同时清空当前字形、原图与效果预览、候选标题、处理记录及原图缓存，不得继续显示刚完成的最后一张图片；搜索或其他筛选导致无可见字形时复用同一完整清空逻辑。
- 自动优化零安全候选时保留实际处理结果并显示为红橙色“结构需核对”，候选提示、右侧方案详情和保存确认均显示原尺寸复核原因及“得分不代表结构安全”；风险元数据同时写入候选顶层和持久化方案。批量采用风险候选后正常推进到待审核并单独计数；手工审核列表对仍处于待审核的风险字形显示警告色、文字标记和具体原因，审核通过后取消风险显示。
- “无学习模型”默认引擎和学习模型适配协议已经接入；模型输出仍经过统一传统寻优，探索分支固定完整模型上下文。
- 寻优器已加入自适应尺度、低对比专用路线、双阈值种子重建、方向无关的主体外污染过滤、分层快速束搜索、结构保护、Pareto 排序和全尺寸复评分；重污染下低可信端点与孔洞降为软约束，通用路线足够时跳过强去杂搜索，候选不足时再增量展开候补派生。测试1的 `以-0001.tif` 已产生清除底部污染带和孤立小点的候选，得分 92.7。
- 密集细噪已接入“笔画尺度核心重建”三档候选：保形档完整保留证据不足的歧义部件，不设固定数量上限；均衡和强清理档仅使用墨色与形态均可信的主体。浅墨纹理点画、斜向短笔、连接主体的细长末笔、窄桥和孔洞分别保护；最大粗污染跨越图像同一维两侧且与内区字形分离时，视为主体锚点歧义并逐像素回退。三档均不新增墨点。
- 笔画尺度分析先以面积、尺度和位置完成廉价门控，候选任务内按规范化图像内容复用分析；缓存最多 8 项、32 MiB，命中返回隔离副本并在任务结束后清空。测试1的 `乃-0001.tif` 首选候选得分 98.5，由 491 个原始前景域收敛为 2 个主体域，底边与左边粗噪团已清除；本机连续实测候选生成由缓存前约 5.4～5.6 秒降至约 4.5～4.8 秒。
- 自动优化与手工审核批处理已改为按字形直接提交 SQLite，不再生成 JSONL 或整库 JSON 检查点。所有首次生成和覆盖路径都使用带新旧 MD5 的三阶段图片事务；提交结果不确定时立即停止整批并保留恢复材料，下次打开字库时恢复。两类批处理继续共用同库独占锁、约 10 Hz 进度限频、64 KiB 日志缓冲和阶段耗时汇总。自动优化批量只包装最终寻优结果并在 PNG 编码时同步计算 MD5；手工审核继续使用同尺寸无变换、单次包围盒和已有人工稿快速路径。
- 整体协调已重构为三栏双模式工作台：左侧只纳入已完成手工审核的字形，采用带真实白底缩略图的“字形与文件 / 状态与提示”两列分组树；第一行显示`待协调 / 已协调`，第二行显示“未保存修改”和墨色等辅助提示。整库比较和单字精调共享完整变换状态、撤销记录和高质量渲染器。比较模式严格保存当前页，精调模式严格保存当前字形，全库处理仅由“完成整体协调”执行。批量保存失败时恢复旧成品、SQLite 和内存状态。
- 新版手工审核页已完成三栏工作台、同字父子分组与真实阶段缩略图、完整自由变换、像素编辑、保存回滚、绘图板压感与笔尾擦除和最小窗口适配；左栏只纳入已完成自动优化的字形，采用“字形与文件 / 状态与提示”两列分组树，第一行显示`待审核 / 已审核`，第二行显示结构风险等辅助提示，两行分别保持原有颜色。状态筛选固定为“全部（本阶段） / 待审核 / 已审核”，默认“全部（本阶段）”，搜索用于导航并保留排序；审核完成后本页显示“已审核”，辅助提示不参与筛选。父组不会夺走当前字形，大列表按可见区后台加载缩略图。田字格改为浅红色，外框使用实线，内部中心线和对角线使用虚线；外围 130% 红框不再显示，但保留同中心 130% 不可见越界编辑范围。按住空格时可用鼠标或绘图板笔尖临时抓手平移，松开空格恢复当前工具，且不修改稿件或撤销记录。自由变换已补齐水平或垂直拉伸与压缩、`Shift`/`Alt` 缩放、四角透视扭曲、四边斜切、旋转局部轴、固定锚点、退化防护及不裁切输出；手工审核与整体协调中的等比缩放、水平拉伸和垂直拉伸统一限制为 `5%–500%`，滑块以 `100%` 为轨道几何中点，页面、画布拖拽和保存服务保持相同边界。自由变换、画笔和橡皮均提供随操作变化的光标，笔宽支持滚轮及全页面 `[`、`]` 快捷键；右侧不显示自由变换、画笔或橡皮的常驻操作说明，参数从顶部按固定间距紧凑排列，参数栈随当前工具内容收缩，“当前稿件”紧接参数显示。活动笔画越界读取、连续多方向扩图和透明区空白橡皮均已补充状态与撤销保护。离开页面或切换字形、切换字库时的确认按钮统一为“放弃修改”和“取消”；放弃会恢复最后保存基线、清空撤销和重做记录并清除未保存状态，切换到自动优化等其他页面后再次进入手工审核不会重复提示，取消则保留草稿。画笔参数在 `1600×900` 与 `1100×720` 截图检查中均无控件重叠、工具栏省略或参数输入裁切。绘图板行为已通过合成 Qt 输入事件验证，仍需在目标机器使用真实硬件试画确认。
- 字库导出左栏只纳入已完成手工审核、已经进入整体协调阶段的字形，采用“字形与文件 / 协调状态 / 提示 / 导出”父子分组树；状态筛选固定为“全部（本阶段） / 待协调 / 已协调”，辅助提示只展示、不筛选。“可导出 / 不可导出”由独立导出审计给出，不得替代协调状态；已有成品显示最终图片，待协调字形显示当前有效审核来源。可见区与后续 8 项使用最多 2 个后台线程解码，图标缓存限制为 256 项，并以路径、修改时间、文件大小和页面代次隔离旧结果。父组不可成为当前字形，列表与中间画廊选择保持双向同步。中间画廊以字库“画布宽 × 画布高”作为田字格逻辑尺寸，外围白色工作区为两轴各 `130%` 且保持同中心；普通成品与记录了对称扩展的成品均按同一逻辑像素尺度绘制，旧成品缺少“实际画布”记录时只在田字格内等比适配，不得强制拉伸。导出前必须由服务层按最终命名规则预检目标目录；同名文件由主线程逐项提示“覆盖 / 跳过 / 取消”，并提供“为所有项目执行此操作”复选框。勾选后仅对本次剩余冲突复用覆盖或跳过决定；取消不得启动后台任务。覆盖提交前必须备份全部旧文件，任一提交失败均恢复旧文件并删除本批新增文件；预检后目标文件发生变化或出现未授权的新冲突时整批拒绝提交。
- `tests/` 当前完整回归为 `750/750` 通过；覆盖程序级五类配置统一存储、字库创建、旧数据一次导入、重复执行、失败不安装、语义与数据库完整性核对、按字形增量提交、无 JSONL 批处理、首页摘要缓存和三个工作台的状态投影；
- 批处理与恢复专项覆盖单字局部快照、SQLite 增量提交、同库独占锁、提交不确定立即停止、旧恢复日志只读重放，以及图片事务的 `rollback / rollforward / cleanup`、部分安装前滚、损坏新图回滚、清理失败后再次保存和活动锁冲突。绘图板相关行为已通过合成 Qt 输入事件验证，仍需在目标机器上用真实硬件试画确认。
- 最近一次界面验证使用 410×135 像素歧义卡片，红色人工输入框完整显示，已取消“最终字符不……”可见校验文字。
- `字库/测试/测试.json` 与 `字库/测试/测试.json.bak` 包含现有测试进度，未经用户确认不得还原。
- 审批服务曾间歇返回 503；最后一次测试和截图验证已正常获批，若新会话复现应直接说明审批状态。

---

## 13. 文档维护规则

出现以下情况时必须同步更新本文档：

- 字库数据库表、字段或状态变化；
- 六阶段目录语义变化；
- 服务接口或依赖方向变化；
- 页面新增、删除或完成迁移；
- 算法、评分或自动寻优语义变化；
- 画布渲染架构变化；
- 兼容、打包或项目隔离规则变化。

历史资料只追加说明或归档，不再覆盖现行规范。本文档描述“应该保持的真实行为”，代码实现和文档必须在同一功能提交中保持一致。
