# 字库编辑 PySide6 开发说明书

> 文档状态：现行开发规范
>
> 基准日期：2026-08-10
>
> 适用项目：`D:\Code\FontEditor-PySide6`
>
> 旧版参考：`D:\Code\FontEditor`（只读，禁止在迁移过程中修改）

---

## 1. 文档定位

本文档是 FontEditor-PySide6 的现行开发依据，描述当前已确认的业务行为、数据兼容要求、PySide6 技术路线和迁移顺序。

资料优先级如下：

1. 本文档中的明确约束；
2. 新项目 `core`、`data`、`services`、`utils` 的实际接口；
3. 旧版 `reference\tk_ui` 的现行业务行为；
4. `reference\旧版开发进度.md` 的完成记录；
5. `reference\字库编辑_V2_Tkinter历史开发说明书.md` 的历史需求。

旧版说明书只用于追溯早期设计，不再作为技术架构、目录结构或完成状态的权威来源。若本文档与实际代码不一致，应先核对旧版稳定程序的真实行为，再更新本文档，禁止凭旧说明书覆盖现有业务逻辑。

---

## 2. 项目目标

在不影响 Tkinter 旧版的前提下，使用 PySide6 重建字库编辑程序，并保持既有字库数据、去杂算法、自动寻优、审核流程和导出结果兼容。

核心目标：

- 解决 Tkinter 画笔、橡皮擦的图像上传与刷新延迟；
- 保留旧版已经稳定的业务规则和数据格式；
- UI 层全面采用 PySide6，不在新主程序中继续使用 Tkinter；
- 业务逻辑与界面解耦，避免再次形成超大窗口类；
- 新旧项目互相独立，可分别开发、运行和回退。

当前新仓库处于“迁移准备基线”：业务层和参考资料已复制，PySide6 主程序尚未建立，不能将已复制的 Tk UI 误认为已完成迁移。

---

## 3. 项目隔离原则

### 3.1 目录边界

- 新版工作目录：`D:\Code\FontEditor-PySide6`
- 旧版工作目录：`D:\Code\FontEditor`
- 新版只允许修改新版目录中的文件。
- 旧版源码、虚拟环境、字库和 Git 仓库均保持原状。
- 如需参考旧版实现，优先读取新版中的 `reference` 副本；确需核对旧项目时只读访问。

### 3.2 Git 规则

- 新版拥有独立 Git 仓库，不关联旧版提交历史。
- 每批功能完成并验证后执行本地提交。
- 未经明确要求不得推送远程仓库。
- 不提交 `.venv`、缓存、日志、构建产物或临时数据。

### 3.3 打包规则

- 开发阶段不自动执行 PyInstaller 打包。
- 只有用户明确要求时才制作或更新便携版。

---

## 4. 技术架构

### 4.1 技术栈

- Python 3.12
- PySide6 6.10.3
- Pillow
- NumPy
- OpenCV
- scikit-image
- SciPy
- send2trash

依赖以根目录 `requirements.txt` 为准，使用新项目自己的 `.venv`，不得复用或修改旧版虚拟环境。

### 4.2 分层结构

```text
FontEditor-PySide6/
├─ main.py                    # 待建立：PySide6 应用入口
├─ ui/                        # 待建立：PySide6 页面、组件、对话框
├─ core/                      # 图像管线、算法、评分、自动寻优
├─ data/                      # 字库 JSON 存储和数据访问
├─ services/                  # 导入、审核、字形、设置、清理等业务服务
├─ utils/                     # 图像、路径、日志、字符等通用工具
├─ 配置/                      # 算法注册表
├─ 字库/测试/                 # 独立小型兼容测试数据
├─ prototype/                 # 已验证的 PySide6 画布性能原型
└─ reference/                 # 旧版代码和历史资料，只读参考
```

依赖方向：

```text
ui → services → data/core/utils
```

约束：

- `ui` 不直接拼装字库 JSON，不复制业务校验规则；
- `services` 不依赖 PySide6 控件；
- `core` 中的算法保持可独立测试；
- 后台任务只处理数据，所有控件更新回到 Qt 主线程；
- 新 PySide6 页面不得导入 `reference\tk_ui`。

### 4.3 用户可见内容

界面文案、提示、日志、进度消息、文档和代码注释统一使用简体中文。程序内部类名、函数名和变量名使用英文，并保持既有业务层风格。

所有功能页的返回按钮统一使用“返回首页”，不得使用单独的“返回”。

---

## 5. 字库数据规范

### 5.1 六阶段目录

每个字库目录采用以下阶段：

| 阶段 | 目录 | 用途 |
|---|---|---|
| 1 | `01_原始图` | 用户导入文件的原始副本 |
| 2 | `02_原图` | 灰度、极性和墨色等预处理固化结果 |
| 3 | `03_优化稿` | 模板管线或逐字自动寻优结果 |
| 4 | `04_编辑稿` | 手工审核与画布编辑结果 |
| 5 | `05_协调稿` | 整体协调后的结果 |
| 6 | `06_成品` | 最终输出结果 |

历史四目录设计已经废弃。新增功能必须识别六阶段目录；如需兼容历史字库，应通过数据访问层集中处理，不能在各页面散布路径判断。

### 5.2 字库清单

当前字库清单文件为字库目录下的 JSON 文件。`GlyphStore` 负责原子读写并维护结构，核心字段包括：

- `名称`
- `创建时间`
- `最后修改`
- `模板`
- `规格`
- `单字寻优`
- `目标跑分`
- `未达标策略`
- `字形`

规格包含：

- `目标宽度`
- `目标高度`
- `水平方向`
- `垂直方向`
- `保持比例`

字形记录以字符为键，每个字符保存变体列表。变体常用字段包括：

- `序号`
- `文件名`
- `原始文件名`
- `原始相对路径`
- `路径`
- `状态`
- `得分`
- `方案`

### 5.3 状态与阶段映射

当前数据层支持的状态：

| 状态 | 对应目录 |
|---|---|
| `原图` | `02_原图` |
| `待优化` | `02_原图` |
| `待复核` | `03_优化稿` |
| `待审核` | `03_优化稿` |
| `已审核` | `04_编辑稿` |
| `已协调` | `05_协调稿` |
| `已确认` | `06_成品` |

状态变更必须通过 `GlyphStore.update_variant_status()` 或业务服务完成，以确保文件复制和 JSON 更新一致。禁止仅修改界面状态或仅改 JSON。

### 5.4 数据安全

- JSON 保存使用临时文件加替换的原子写入方式；
- 字库写操作遵循“文件成功后再更新数据”的顺序；
- 删除优先进入回收站，不直接永久删除；
- 导入、审核、协调和导出失败时不得留下半更新状态；
- 测试不得改写 `字库\测试` 的基准数据，需写入时先复制到临时字库。

---

## 6. 已有业务能力

以下能力已存在于复制的业务层或旧版稳定实现中，PySide6 迁移时应复用或等价实现，不应按早期文档重新设计。

### 6.1 字库管理

- 新建字库；
- 删除字库并移入回收站；
- 修改字库规格；
- 列出字库、统计字符数和图片数；
- 自动修复缺失阶段目录；
- 字图追加导入；
- 标准字库导入与导出。

### 6.2 图片导入

`ImportService` 已支持：

- 递归扫描图片；
- PNG、JPG、JPEG、TIF、TIFF；
- 从文件名提取字符；
- 复制原始图；
- 固化灰度、极性和墨色预处理；
- 模板批量处理；
- 单字逐图自动寻优；
- 目标分数和未达标策略；
- 进度回调和取消；
- 追加导入时避免重复源路径。

两类新建模式：

1. 模板模式：按选定模板批量运行统一管线；
2. 单字寻优模式：每张图片独立选择最优方案，记录得分与方案。

### 6.3 图像管线

`core.pipeline.run_pipeline()` 是当前管线入口。处理顺序为：

1. 预处理；
2. L1 降噪；
3. L2 背景校正；
4. L3 二值化；
5. L4 形态学；
6. L5 连通域过滤；
7. 透明化输出。

预处理支持转灰度、反相和墨色归一。模板和算法参数由 `配置\算法注册表.json` 及现有方案结构驱动。

必须保留的重要行为：

- 导入阶段已固化预处理后，后续管线不得重复反相或重复墨色归一；
- 拓片类均匀浓墨场景优先使用 Otsu，不能默认使用会削薄粗笔画的 Sauvola；
- L5 删除区必须在灰度图中真正置白，不能只修改随后会丢失的 Alpha；
- 相对面积过滤按文字主体面积推导，并与绝对最小面积共同约束；
- 算法注册表和内置模板是新 UI 的配置来源，不在界面中硬编码两套定义。

### 6.4 自动寻优与评分

`core.optimizer` 提供：

- 噪点分析；
- 缩略图生成；
- 候选方案构建；
- 基础候选评分；
- 梯度派生候选；
- 达标提前停止；
- 最优图、最优方案、得分和达标状态返回。

迁移时保持以下语义：

- 目标跑分是质量线，不等同于绝对准入线；
- 未达标策略支持“入库并标记”和“跳过不入库”；
- 未达标但入库的图片状态为 `待复核`；
- 审核页支持低分优先；
- 每字得分和方案必须持久化。

评分逻辑的重要约束：

- 参考文字主体使用主要连通域，避免把散点当作应保留笔画；
- 参考掩码连通域需满足相对面积下限；
- 非文字连通域按尺寸分级惩罚；
- 明显浅于主体文字的贴字噪点仍需惩罚；
- 诊断归档图片时不能再次墨色归一，否则会污染评分结论。

### 6.5 审核与协调

已有业务服务包括：

- `ReviewService`：审核统计、分页查询、状态筛选和审核结果提交；
- `GlyphService`：读取变体、保存编辑稿、完成审核、复制最终结果；
- `CoordinationService`：协调稿生成和状态推进；
- `CleanupService`：中间文件清理；
- `SettingsService`：设置读写。

PySide6 页面应调用这些服务，不应直接复刻文件操作。

---

## 7. PySide6 界面范围

### 7.1 页面清单

按旧版现有能力迁移：

1. 首页与字库列表；
2. 新建字库；
3. 字图追加导入；
4. 字库规格修改；
5. 模板生成与算法配置；
6. 字库审核；
7. 手工审核；
8. 整体协调；
9. 最终确认与导出；
10. 设置；
11. 字库说明；
12. 模型与算法说明。

界面视觉可按 PySide6 重新设计，但业务语义、状态推进、文件结果和用户确认步骤必须与稳定版兼容。

### 7.2 主窗口建议

- 使用单一 `QApplication`；
- 主窗口建议使用 `QMainWindow`；
- 页面切换建议使用 `QStackedWidget` 或路由控制器；
- 全局主题集中管理，禁止每个页面重复硬编码完整样式表；
- 长任务使用 `QThreadPool/QRunnable` 或工作对象配合 `QThread`；
- 工作线程通过信号上报进度、结果和错误；
- 所有对话框、提示和进度文案使用中文。

---

## 8. 手工审核画布规范

手工审核画布是第一优先级，也是采用 PySide6 的主要性能原因。

### 8.1 已验证结论

独立原型 `prototype\PySide6画布性能测试.py` 已在真实机器上验证流畅。测试证明持久 `QImage`、`QPainter` 和脏矩形刷新可以绕开旧版 Tkinter 的 `PhotoImage` 创建及上传瓶颈。

### 8.2 必须采用的渲染架构

- 高清数据层使用持久 `QImage`；
- 画笔和橡皮擦直接修改工作图像的局部区域；
- 绘制使用 `QPainter`；
- 仅调用 `update(dirty_rect)` 请求脏矩形重绘；
- `paintEvent()` 只绘制需要显示的内容，不在每帧执行完整 Pillow 转换；
- 屏幕缩放结果允许缓存；
- 一笔结束时只创建撤销快照或命令，不触发无必要的整图重建；
- 光标、控制柄和辅助线作为独立覆盖层绘制。

### 8.3 禁止回退的做法

- 每个鼠标移动事件创建整张 `QImage` 或 `QPixmap`；
- 每帧执行 Pillow → QImage → QPixmap 的整图转换；
- 每帧对整张高清图做 LANCZOS 缩放；
- 松笔时无条件完整重渲染；
- 在主线程执行批量导入或长时间算法任务；
- 为了“迁移方便”在 Qt 事件循环内嵌入 Tkinter 画布。

### 8.4 功能兼容要求

手工审核最终需覆盖：

- 字形与变体选择；
- 三栏核对和分页；
- 移动、缩放、旋转和扭曲；
- 画笔、橡皮擦和笔刷大小；
- 田字格、边界框、控制柄和实时预览；
- 撤销、重做和重置；
- 自动对齐与缩放；
- 保存编辑稿及状态推进；
- 待复核、待审核和低分优先；
- 中文性能日志。

OpenCV 透视变换行为应继续保留。旧版曾使用 Pillow 透视变换并出现奇异分母和 NaN，大角度扭曲不得退回该实现。

---

## 9. 迁移阶段与验收

### 阶段 1：应用骨架

内容：

- 建立 `main.py`、`QApplication` 和主窗口；
- 建立主题、页面路由、异常处理和日志入口；
- 接通测试字库读取；
- 建立后台任务统一封装。

验收：程序可独立启动和退出，首页能读取 `字库\测试`，不导入 Tkinter，不访问旧版目录。

### 阶段 2：手工审核画布

内容：

- 从已验证原型提炼可复用画布组件；
- 实现载图、显示、画笔、橡皮擦和脏矩形刷新；
- 迁移变换、撤销、保存和审核状态；
- 接入 `GlyphService` 与 `ReviewService`。

验收：真实大图连续绘制保持流畅，保存结果和旧版兼容，原图不被覆盖，性能日志无秒级整图转换。

### 阶段 3：审核工作流

内容：

- 迁移字库审核、三栏核对、筛选、分页和低分优先；
- 连接手工审核页面；
- 完成待复核、待审核、已审核状态闭环。

验收：可在复制的临时测试字库完成一轮审核，目录和 JSON 状态同步一致。

### 阶段 4：字库管理与导入

内容：

- 首页、新建字库、追加导入和规格修改；
- 模板选择、单字寻优、进度、取消和错误报告；
- 保持六阶段目录和现有 JSON 格式。

验收：模板模式与单字模式均可端到端创建字库；中途取消不产生损坏数据。

### 阶段 5：模板与算法配置

内容：

- 按注册表生成预处理和五层算法界面；
- 参数编辑、模板保存、说明和预览；
- 复用现有管线与优化器。

验收：同一输入与同一方案在新旧版产生等价结果；不重复执行已固化预处理。

### 阶段 6：协调、确认与导出

内容：

- 整体协调；
- 最终确认；
- 标准字库导入导出；
- 中间文件清理。

验收：状态从已审核推进到已协调、已确认，最终文件完整且 JSON 无悬空路径。

### 阶段 7：回归与交付

内容：

- 功能回归、异常路径、边界图和性能验证；
- 更新用户说明；
- 用户明确要求后再制作便携版。

验收：测试字库完整跑通；旧版项目 Git 状态和文件哈希无变化；新版无 Tkinter 运行依赖。

---

## 10. 测试要求

### 10.1 基础测试

- 业务层模块导入和语法检查；
- 空图、全白、全黑、极小图和超大图；
- PNG、JPEG、TIF、TIFF；
- 黑底白字和白底黑字；
- 中文路径、长路径和重复文件名；
- 取消、异常、磁盘写入失败和非法 JSON；
- 多变体字符和缺失阶段目录。

### 10.2 算法回归

- Otsu、百分位、Sauvola、黑帽和连通域过滤；
- 雪花椒盐噪点；
- 浅灰贴字噪点；
- 粗笔画保留；
- 墨色归一与逐图极性判断；
- 自动候选、派生候选、目标分数和未达标策略；
- 同一固定样本的新旧结果对比。

### 10.3 画布性能

日志至少记录：

- 图片载入；
- 首帧显示；
- 每笔总耗时；
- 局部像素处理；
- 脏矩形刷新；
- 松笔提交；
- 保存和状态更新。

重点观察平均值、中位数、P95、最大值及超过 16.67 ms 的帧数。不得只凭主观流畅度删除性能日志；稳定后可降低日志级别。

---

## 11. 明确废弃或不采用的设计

- Tkinter 作为新版主界面技术；
- Tk `Canvas` + `PhotoImage` 的像素编辑刷新链路；
- 四阶段字库目录；
- 旧版平铺模板格式作为新模板编辑模型；
- UI 内复制管线、评分或状态推进逻辑；
- 每笔完整高清重渲染；
- 使用固定延时猜测布局完成；
- Pillow 透视变换用于当前扭曲功能；
- 自动修改旧版代码或旧版业务字库；
- 未经要求自动打包或推送。

---

## 12. 新会话开始步骤

新会话进入 `D:\Code\FontEditor-PySide6` 后依次执行：

1. 阅读 `AGENTS.md`；
2. 阅读本文档；
3. 阅读 `README.md`；
4. 查看 Git 状态；
5. 只在需要时查阅 `reference`；
6. 从“阶段 1：应用骨架”继续，不修改旧版目录。

建议的新会话指令：

> 请继续开发 `D:\Code\FontEditor-PySide6`。先阅读 `AGENTS.md`、`字库编辑_PySide6_开发说明书.md` 和 `README.md`，核对当前 Git 状态，然后按开发说明书从 PySide6 应用骨架和手工审核页面开始。`D:\Code\FontEditor` 只读且必须保持不变。

---

## 13. 文档维护规则

出现以下情况时必须同步更新本文档：

- 字库 JSON 字段或状态变化；
- 六阶段目录语义变化；
- 服务接口或依赖方向变化；
- 页面新增、删除或完成迁移；
- 算法、评分或自动寻优语义变化；
- 画布渲染架构变化；
- 兼容、打包或项目隔离规则变化。

历史资料只追加说明或归档，不再覆盖现行规范。本文档描述“应该保持的真实行为”，代码实现和文档必须在同一功能提交中保持一致。
