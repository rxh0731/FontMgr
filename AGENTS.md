# FontEditor-PySide6 开发约定

## 项目目标

本项目是 `D:\Code\FontEditor` 的独立 PySide6 改造版。旧版仅作业务行为和界面参考，禁止从新项目反向修改旧版目录。

## 开发原则

- 所有用户可见内容、代码注释、日志、进度信息和文档使用简体中文。
- 函数名、变量名、属性名使用英文，并保持 Python 现有风格。
- 优先复用 `core`、`data`、`services`、`utils` 中与 GUI 无关的业务代码。
- 新界面使用 PySide6，不在正式代码中继续引入 Tkinter。
- 画布采用持久 `QImage`、`QPainter` 和脏矩形 `update(QRect)`，避免每帧整图格式转换。
- 功能页面的返回按钮统一使用“返回首页”。
- 不自动打包便携版，除非用户明确要求。
- 修改完成后可提交到本项目本地 Git，但未经明确要求不得推送。

## 目录说明

- `core/`：图像算法、评分、管线和算法注册表。
- `data/`：配置、字库、模板、撤销和日志等数据层。
- `services/`：导入、导出、字形、优化和调整服务。
- `utils/`：文件与异常处理工具。
- `ui/`：新的 PySide6 正式界面，当前留空待开发。
- `prototype/`：已实测流畅的 PySide6 画布原型。
- `reference/tk_ui/`：旧版 Tk 界面只读参考副本，不作为新界面继续扩展。
- `reference/main_tk.py`：旧版入口参考副本。
- `字库/测试/`：独立小型测试字库，不连接旧版运行数据。

## 首要开发顺序

1. 建立 PySide6 主窗口、首页导航和统一主题。
2. 迁移手工审核页面，以 `prototype/PySide6画布性能测试.py` 为画布性能基线。
3. 接入字形选择、变体选择、撤销、保存及调整工具。
4. 迁移模板生成、字库管理、导入和导出页面。
5. 完成功能回归和性能日志对比。
